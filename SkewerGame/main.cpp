#include <iostream>
#include <vector>
#include <conio.h>
#include "GameManager.h"
#include "mtg.h"

// 꼬치 게임
// 시간 안에 꼬치를 알맞게 끼우는 게임
// 1. q, w, e, r, t 5종류의 꼬치 재료가 있음
// 2. 한 턴에 3~5개의 꼬치를 제한시간 안에 순서에 맞게 모두 끼워야 함
// 3. 제한 시간은 한 턴이 지날 때마다 리셋 or 전체 n초 안에 (맞추면 시간 증가) 

void Draw()
{

}

int main()
{
	GameManager* gm = GameManager::GetInstance();

	return 0;
}